# ============================================================================
# CRITICAL: CUDA INSTALLATION INSTRUCTIONS - DO NOT SKIP
# ============================================================================
# STEP 1: Install PyTorch with CUDA 11.8 support FIRST:
#
#   ./venv/Scripts/pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# STEP 2: Then install other packages:
#
#   ./venv/Scripts/pip install -r requirements.txt
#
# ============================================================================
# WARNINGS:
# - Do NOT run "pip install chatterbox-tts" by itself - it will install incompatible PyTorch versions
# - Do NOT install torch/torchvision/torchaudio without the --index-url flag
# - Current working setup: PyTorch 2.7.1+cu118, TorchVision 0.22.1+cu118, TorchAudio 2.7.1+cu118
# ============================================================================

# NumPy version must be compatible with both PyTorch and Chatterbox
numpy>=1.24.0,<1.26.0

diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0
Pillow>=10.0.0
tqdm>=4.66.0

# xformers must be installed separately after torch
# See installation instructions in README_IMAGE_GEN.md

# Text-to-Speech (Resemble AI Chatterbox TTS)
chatterbox-tts>=0.1.6      # Resemble AI Chatterbox TTS (~350M-500M model download on first run)
pydub>=0.25.1              # Audio manipulation (WAV to MP3 conversion)
soundfile>=0.12.1          # Audio file I/O
scipy>=1.10.0              # Audio processing utilities

# Video Generation (MoviePy)
moviepy>=2.1.0             # Video editing library for creating YouTube videos (API v2.x)
imageio>=2.31.1            # Image I/O for MoviePy
imageio-ffmpeg>=0.6.0      # FFmpeg binaries for MoviePy
python-dotenv>=1.0.0       # Environment variable management (MoviePy dependency)

# Development Tools
debugpy>=1.8.0             # VSCode Python debugger
